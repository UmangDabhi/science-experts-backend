# =============================================================================
# SCIENCE EXPERTS BACKEND - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# =============================================================================

# Application
NODE_ENV=development
PORT=5000
BACKEND_URL=http://localhost:5000
FRONTEND_URL=http://localhost:3000

# =============================================================================
# DATABASE - Neon.tech PostgreSQL (Serverless)
# =============================================================================
# Get these from: https://console.neon.tech/
DB_HOST=your-project.neon.tech
DB_PORT=5432
DB_USERNAME=your_username
DB_PASSWORD=your_password
DB_DATABASE=scienceexperts_db
DB_SSL=true

# =============================================================================
# CLOUDFLARE R2 STORAGE (S3-Compatible Object Storage)
# =============================================================================
# Create R2 bucket at: https://dash.cloudflare.com/
# Generate API tokens from: R2 > Manage R2 API Tokens
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_BUCKET_NAME=scienceexperts-uploads
R2_PUBLIC_URL=https://pub-your-account-id.r2.dev

# Optional: Custom domain for R2 (if you set one up)
# R2_PUBLIC_URL=https://cdn.yourdomain.com

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRATION=7d

# =============================================================================
# PAYMENT - RAZORPAY
# =============================================================================
# Get credentials from: https://dashboard.razorpay.com/
RAZORPAY_MODE=sandbox
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# =============================================================================
# EMAIL - SMTP Configuration
# =============================================================================
# Using Gmail SMTP (or any SMTP provider) 

# For Gmail: Generate app password at https://myaccount.google.com/apppasswords
# For other providers, use your SMTP credentials

# =============================================================================
# REDIS (Optional - for caching/sessions)
# =============================================================================
# Redis is optional. If not provided, in-memory caching will be used
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password

# =============================================================================
# SIGNED URLS
# =============================================================================
SIGNED_URL_EXPIRATION_SECONDS=3600

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# RENDER DEPLOYMENT:
# - Set these environment variables in Render Dashboard
# - BACKEND_URL will be: https://your-service.onrender.com
#
# NEON.TECH DATABASE:
# 1. Create free PostgreSQL database at https://neon.tech/
# 2. Copy connection string from Neon dashboard
# 3. Database auto-pauses when inactive (free tier)
#
# CLOUDFLARE R2:
# 1. Create R2 bucket at https://dash.cloudflare.com/
# 2. Generate R2 API tokens
# 3. No egress fees, global CDN
# 4. S3-compatible API (works with AWS SDK)
#
# VERCEL FRONTEND:
# - Set FRONTEND_URL to your Vercel deployment URL
# - Example: https://your-app.vercel.app
#
# =============================================================================
